#include <locale.h>
#include <stdio.h>
#include <stdlib.h>
#include <string.h>
int check(char str[]);
void transformation_A_a();
void deleting_punctuation_marks(char *str);
void word_comparison(char *ptr);